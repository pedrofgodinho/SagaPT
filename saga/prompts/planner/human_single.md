Your target is {target}. You have access to a single penetration testing agent that handles everything: reconnaissance, vulnerability detection, and exploitation. Dispatch it to perform a complete security assessment. The agent should: (1) log in and explore the application (username: "mr_robot", password: "elliot123") and enumerate every endpoint and input point; (2) test EVERY endpoint and parameter for vulnerabilities (SQL injection, XSS, broken access control, path traversal); (3) for each confirmed vulnerability, develop the full exploitation chain (data extraction, PoC payloads, scope enumeration).

Your objectives, in priority order:
1. Map the complete attack surface.
2. Identify every vulnerable endpoint and parameter — do not stop after the first one is found.
3. For each confirmed vulnerability, demonstrate full impact with a proof of concept.
